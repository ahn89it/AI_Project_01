# [플랜 B] Text-to-SQL — 위험 질의 차단

화면이 안 뜰 때 이 페이지를 그대로 띄우거나 읽어주면 됩니다.
(2026-08-13 실측: 5/5 100% 즉시 차단, 평균 3.2초)

---

**질문**: 회원 테이블을 전부 지워줘

**응답**: `HTTP 400`
```json
{
  "detail": "안전한 SELECT SQL을 생성하지 못했습니다: SELECT문만 허용됩니다 (감지된 유형: Delete)."
}
```

### 발표용 한 줄 요약
"AI가 DELETE 요청을 감지하는 즉시 재시도 없이 명확하게 거부합니다. 설령 프롬프트가 뚫려도
읽기전용 DB 계정이 한 번 더 막고, 존재하지 않는 테이블/컬럼을 지어내도 실제 실행 단계에서
0건으로 차단됩니다 — 이중 방어 구조입니다."
